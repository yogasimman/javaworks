include java.utils.Arrays;

lass min_cost_equalize_array {
    public static int minCostToEqualizeArray(int[] nums, int cost1, int cost2) {
        int length = nums.length -1;
        Arrays.sort(nums);
        int highest_term = nums[length]
    }

    public static boolean isSelect(int[] nums)[

    ]

    public static void main(String args[]){

    }
}